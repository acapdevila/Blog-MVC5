@using Microsoft.AspNet.Identity

@{
    var currentController = (string)ViewContext.RouteData.Values["Controller"];
    const string activeClass = "active";
}

@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
    @Html.AntiForgeryToken()
        <br />
    <ul class="list-inline text-uppercase smallest" style="padding-right:10px">
        <li class="@(currentController == "Manage" ? activeClass : string.Empty)">
            @Html.ActionLink("Hola " + User.Identity.GetUserName() + "!", "Index", "Manage", routeValues: null, htmlAttributes: new {title = "Manage"})
        </li>
        @*<li class="@(currentController == "Account" ? activeClass : string.Empty)">
            @Html.ActionLink("Register", "Register", "Account", routeValues: null, htmlAttributes: new {id = "registerLink"})
        </li>*@
        <li><a href="javascript:document.getElementById('logoutForm').submit()">Salir</a></li>
    </ul>
    }
}
else
{
    <br />
    <ul class="list-inline text-uppercase smallest" style="padding-right:10px">
        <li class="@(currentController == "Account" ? activeClass : string.Empty)">
            @Html.ActionLink("Acceso", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })
        </li>
    </ul>
}
